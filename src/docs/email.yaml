# @see src/routes/email.js
tags:
  - name: Email
    description: Email sending

paths:
  /send-email:
    post:
      summary: Send an email
      tags: [Email]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email, subject, text]
              properties:
                email:
                  type: string
                subject:
                  type: string
                text:
                  type: string
      responses:
        '204':
          description: Email sent successfully
